<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - NASA APOD</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: system-ui, -apple-system, sans-serif; background: #0b0d17; color: #e0e0e0; }
        .container { max-width: 1200px; margin: 0 auto; padding: 20px; }
        nav { background: #13162b; border-bottom: 1px solid #2a2d45; padding: 1rem 0; }
        .nav-inner { display: flex; gap: 24px; align-items: center; }
        .brand { font-weight: 700; font-size: 1.2rem; color: #fff; text-decoration: none; margin-right: auto; }
        nav a:not(.brand) { color: #8892b0; text-decoration: none; font-size: 0.95rem; }
        nav a:not(.brand):hover { color: #64ffda; }
        h1 { margin: 0 0 1.5rem; }
        .btn { padding: 10px 20px; background: #1a73e8; color: #fff; border: none; border-radius: 6px; cursor: pointer; }
        .btn:hover { background: #1557b0; }
        .form-group { margin-bottom: 1rem; }
        label { display: block; margin-bottom: 6px; font-weight: 500; }
        input[type="date"] { padding: 8px 12px; border: 1px solid #2a2d45; border-radius: 6px; background: #1a1d30; color: #e0e0e0; }
        .text-danger { color: #ff6b6b; }
        .alert-success { padding: 12px 16px; border-radius: 6px; background: #1b3a2a; color: #64ffda; margin-bottom: 1rem; }
        .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 20px; }
        .card { background: #13162b; border-radius: 10px; overflow: hidden; border: 1px solid #2a2d45; transition: transform 0.2s; }
        .card:hover { transform: translateY(-4px); }
        .card img { width: 100%; height: 200px; object-fit: cover; }
        .card-body { padding: 14px; }
        .card-body h3 { font-size: 1rem; margin-bottom: 6px; color: #fff; }
        .card-body small { color: #8892b0; }
        .empty { text-align: center; padding: 3rem; color: #8892b0; }
    </style>
</head>
<body>
    <nav>
        <div class="container nav-inner">
            <a asp-controller="Home" asp-action="Index" class="brand">&#127756; NASA APOD</a>
            <a asp-controller="Home" asp-action="Index">Gallery</a>
            <a asp-controller="Apod" asp-action="Import">Import</a>
        </div>
    </nav>
    <div class="container">
        @RenderBody()
    </div>
</body>
</html>
